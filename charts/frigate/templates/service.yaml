apiVersion: v1
kind: Service
metadata:
  name: {{ template "frigate.fullname" . }}
  labels:
{{ include "frigate.labels" . | indent 4 }}
{{- if .Values.service.labels }}
{{ toYaml .Values.service.labels | indent 4 }}
{{- end }}
{{- with .Values.service.annotations }}
  annotations:
{{ toYaml . | indent 4 }}
{{- end }}
spec:
{{- if (or (eq .Values.service.type "ClusterIP") (empty .Values.service.type)) }}
  type: ClusterIP
  {{- if .Values.service.clusterIP }}
  clusterIP: {{ .Values.service.clusterIP }}
  {{end}}
{{- else if eq .Values.service.type "LoadBalancer" }}
  type: {{ .Values.service.type }}
  {{- if .Values.service.loadBalancerIP }}
  loadBalancerIP: {{ .Values.service.loadBalancerIP }}
  {{- end }}
  {{- if .Values.service.loadBalancerSourceRanges }}
  loadBalancerSourceRanges:
{{ toYaml .Values.service.loadBalancerSourceRanges | indent 4 }}
  {{- end -}}
{{- else }}
  type: {{ .Values.service.type }}
{{- end }}
  ipFamilyPolicy: {{ .Values.service.ipFamilyPolicy }}
  {{- if .Values.service.ipFamilies }}
  ipFamilies:
  {{- range .Values.service.ipFamilies }}
    - {{ . }}
  {{- end }}
  {{- end }}
{{- if .Values.service.externalIPs }}
  externalIPs:
{{ toYaml .Values.service.externalIPs | indent 4 }}
{{- end }}
  {{- if .Values.service.externalTrafficPolicy }}
  externalTrafficPolicy: {{ .Values.service.externalTrafficPolicy }}
  {{- end }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      protocol: TCP
      targetPort: http
{{- if (and (eq .Values.service.type "NodePort") (not (empty .Values.service.nodePort))) }}
      nodePort: {{.Values.service.nodePort}}
{{- end }}
{{- if .Values.service.rtsp.enabled }}
    - name: rtsp
      port: {{ .Values.service.rtsp.port}}
      protocol: TCP
      targetPort: rtsp
{{- if (and (eq .Values.service.type "NodePort") (not (empty .Values.service.rtsp.nodePort))) }}
      nodePort: {{.Values.service.rtsp.nodePort}}
{{- end }}
{{- end }}
{{ - if .Values.service.webrtc.enabled }}
    - name: webrtc-udp
      port: {{ .Values.service.webrtc.port}}
      protocol: UDP
      targetPort: webrtc-udp
{{- if (and (eq .Values.service.type "NodePort") (not (empty .Values.service.webrtc.nodePort))) }}
      nodePort: {{.Values.service.webrtc.nodePort}}
{{- end }}
    - name: webrtc-tcp
      port: {{ .Values.service.webrtc.port}}
      protocol: TCP
      targetPort: webrtc-tcp
{{- if (and (eq .Values.service.type "NodePort") (not (empty .Values.service.webrtc.nodePort))) }}
      nodePort: {{.Values.service.webrtc.nodePort}}
{{- end }}
{{- end }}
  selector:
    app.kubernetes.io/name: {{ include "frigate.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
